
@{
    ViewData["Title"] = "Index";
}
@using DRRealState.Core.Application.ViewModel.Upgrade
@model List<UpgradeViewModel>

<div class=" float-end justify-content-center">
    <a type="button" class="btn btn-outline-success" asp-controller="Upgrade" asp-action="Form"><i class="fa-solid fa-plus"></i></a>
</div>
<div class="container-fluid">
    <table class="table">
        <thead class="table-striped table-hover" style="background-color: #000; color: white">
            <tr>
                <th scope="col">#</th>
                <th scope="col">Nombre</th>
                <th scope="col">Descripcion</th>
                <th scope="col">Options</th>
            </tr>
        </thead>
        <tbody>
            @foreach (UpgradeViewModel item in Model)
            {
            <tr>
                <td scope="row">@item.Id</td>
                <td>@item.Name</td>

                <td>@item.Description</td>
               
                <td>
                    <div class="d-flex">
                        <a  class="btn btn-sm-primary active text-white" aria-current="page" style="width: 100px; height: 40px" asp-controller="Upgrade" asp-action="Edit" asp-route-id="@item.Id"><i class="fa-solid fa-pencil"></i>Editar</a>
                        <a  class="btn btn-danger text-white" style="width:100px; height: 40px" asp-controller="Upgrade" asp-action="Delete" asp-route-id="@item.Id"><i class="fa-solid fa-trash"></i>Eliminar</a>

                    </div>

                </td>


            </tr>


            }




        </tbody>
    </table>
</div>

